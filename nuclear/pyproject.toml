[project]
name = "nuclear"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.8",
    "mca-tools",
    "numpy>=2.4.3",
    "scipy>=1.17.1",
]

[tool.uv.workspace]
members = [
    "mca-tools",
]

[tool.uv.sources]
mca-tools = { workspace = true }
