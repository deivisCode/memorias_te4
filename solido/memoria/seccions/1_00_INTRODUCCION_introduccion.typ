#import("/estilo_memoria.typ"): *
= Introducción

// Unhas ecuacións para despois

Modelo equivalente
$
    Delta G &= sqrt((a + b cos(theta)^2)^m) \
    a &= phi.alt_a ((alpha^2 beta)/gamma)^(1\/m) \
    b &= phi.alt_0 ((alpha^2 beta)/gamma)^(1\/m)
$ <ec:equivalentes>

Modelo independente
$
    Delta G &= sqrt(a^m)  + sqrt((b cos(theta)^2)^m) \
    a &= phi.alt_a ((alpha^2 beta_a)/gamma_a)^(1\/m) \
    b &= phi.alt_b ((alpha^2 beta_b)/gamma_b)^(1\/m) \
$ <ec:independente>

+ Obter $a,b,m$ para o modelo Equivalente e o modelo Independente.
+ $b$ debería ser o mesmo varias series, dentro do mesmo modelo. Comparar.
+ $m$ debería ser o mesmo varias series, dentro do mesmo modelo. Comparar.
+ O mellor modelo é o que mantén $b, m$ constantes entre series.

#valor("G_0")
